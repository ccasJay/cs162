-  `_start` :  类unix系统中，程序真正的起点
	1. 获取参数
	2. 调用main
	3. 当main返回后，调用exit()结束进程

- `if_(interrupt frame)` : 中断帧，当程序从用户态切换到内核态（比如执行 `int $0x30` 发起系统调用）时，CPU 的所有寄存器（`eax`, `ebx`, `esp`, `eip` 等）会被立刻“拍一张照片”保存到这个结构体里。

- `int $0x30` : **系统调用入口（Software Interrupt）**。
	1. **原理**：x86 汇编指令，用于触发一个编号为 `0x30` (48) 的软中断。
	2. **作用**：在 Pintos 中作为用户程序请求内核服务的“传送门”。
	3. **流程**：用户程序压入参数 -> 执行 `int $0x30` -> CPU 提权并保存现场 -> 内核执行 `syscall_handler`。

- `x/2xw $esp` : x - 查看内存  2 - 看两个单位     w - 每个单位4字节    $esp - top stack  address pointer register
	**目的**：在程序执行到系统调用指令（`int $0x30`）时，通过这个命令确认堆栈顶端确实放着正确的**系统调用号**（如 `SYS_EXIT`）和**参数**（如 `162`）

### 常用调试技巧补完

- **汇编级单步调试**：
  - `si` (stepi)：执行下一条机器指令（会进入函数内部）。
  - `ni` (nexti)：执行下一条机器指令（跳过函数调用）。
  - `p $pc`：打印当前正在执行的指令地址。
  - `x/i $pc`：以汇编指令格式查看当前位置。

- **断点与监视点**：
  - `b *0x080480a0`：在特定的内存地址（汇编指令位置）打断点。
  - `b file.c:42 if x == 5`：设置条件断点，只有当变量 x 为 5 时才停下。
  - `watch *0x12345678`：数据监视点。当该内存地址的内容发生**任何改变**时，程序会自动暂停（调试内核崩溃、指针乱指的利器）。

- **栈与上下文回溯**：
  - `bt` (backtrace)：查看当前的函数调用栈。
  - `f 1` (frame)：切换到栈的第 1 层（查看父函数的局部变量）。
  - `i r` (info registers)：查看所有通用寄存器的值。

- **可视化布局 (TUI 模式)**：
  - `layout src`：显示源码窗口。
  - `layout asm`：同时显示源码和汇编（极其适合调试 System Call）。
  - `layout reg`：显示寄存器实时变化。
  - `Ctrl + L`：当 TUI 界面因程序输出乱码时，用此组合键刷新界面。

- **打印技巧**：
  - `p/x $eax`：以十六进制格式打印 eax 寄存器。
  - `p *(struct thread *)0xc0101000`：强转地址为结构体并打印其所有成员。